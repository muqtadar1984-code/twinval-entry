# Backend API base URL (no trailing slash).
# Dev (with vite proxy): leave empty and the /api proxy in vite.config.ts will hit localhost:8000
# Prod: full origin of the Railway backend, e.g. https://entry-api.twinval.com
VITE_API_BASE_URL=
